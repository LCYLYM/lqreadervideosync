# Reader Sync By 🐟 v0.1.6

## Highlights

- fixed the tutorial overlay focus flow to avoid the `aria-hidden` warning when closing the onboarding dialog
- hardened content-script reconnect behavior so extension hot-reloads no longer surface an uncaught `Extension context invalidated` error as easily
- restored clearer docked-bubble affordances, including the visible collapse label and more explicit expand hinting
- improved article snapshot fallback logging so API failures no longer degrade into `[object Object]`

## Included Assets

- browser extension package ZIP
- sanitized source snapshot ZIP

## Notes

- runtime matching still depends on the live `aim-read` page and its real article API responses
- local video and subtitle processing remain browser-local
- public release contents intentionally exclude local smoke scripts, auth state, test media, proxy-only traces, and private workspace artifacts
