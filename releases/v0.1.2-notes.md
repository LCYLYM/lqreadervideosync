# Reader Sync By 🐟 v0.1.2

## Highlights

- added a first-run player onboarding flow with spotlight guidance, step cards, close/finish persistence, and a manual reopen entry
- improved reader-page refresh so the player can actively rescan `aim-read` tabs and reload unresponsive article tabs before rebinding
- moved the media compatibility and preprocessing panel above the player for a more natural setup order
- added an in-player Picture-in-Picture entry alongside the existing top-level control
- fixed the player-page spacebar double-trigger when the currently focused element is the native video player

## Included Assets

- browser extension package ZIP
- sanitized source snapshot ZIP

## Notes

- runtime matching still depends on the live `aim-read` page and its real article API responses
- local video and subtitle processing remain browser-local
- public release contents intentionally exclude local smoke scripts, auth state, test media, proxy-only traces, and private workspace artifacts
