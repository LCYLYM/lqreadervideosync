# Reader Sync By 🐟 v0.1.5

## Highlights

- fixed the docked reader sync bubble so hover no longer retriggers unstable animations and clicks can reliably expand the card again
- stabilized the reader-page sync overlay by keeping the docked/expanded DOM nodes persistent instead of rebuilding them on every status refresh
- keeps the player-page theme switcher and the Claude-inspired theme system introduced in recent releases

## Included Assets

- browser extension package ZIP
- sanitized source snapshot ZIP

## Notes

- runtime matching still depends on the live `aim-read` page and its real article API responses
- local video and subtitle processing remain browser-local
- public release contents intentionally exclude local smoke scripts, auth state, test media, proxy-only traces, and private workspace artifacts
