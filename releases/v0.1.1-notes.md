# Reader Sync By 🐟 v0.1.1

## Highlights

- improved runtime-only workflow for authenticated `aim-read` reader pages
- hardened reader tab binding recovery across player, background, and content reconnects
- restored full-article snapshot delivery so runtime subtitle manifests build reliably from real article API data
- verified forward sync, reverse paragraph click seek, and reader-page keyboard control in a headed real-site smoke run
- kept the public package sanitized by excluding local smoke tooling, login state, test media, and proxy-only permissions

## Included Assets

- browser extension package ZIP
- sanitized source snapshot ZIP

## Notes

- runtime matching still depends on the live `aim-read` page and its real article API responses
- local video and subtitle processing remain browser-local
- public release contents intentionally exclude author-only test traces and temporary automation artifacts
