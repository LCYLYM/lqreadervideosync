# Reader Sync By 🐟 v0.1.4

## Highlights

- completed the player-page theme switching flow by adding the visible theme toggle entry and wiring it to persisted `system / dark / light` preferences
- keeps the Claude-inspired dark mode styling and the reader-page docked sync status bubble from `v0.1.3`
- updates the player page so the newest theme can actually be switched without editing storage manually

## Included Assets

- browser extension package ZIP
- sanitized source snapshot ZIP

## Notes

- runtime matching still depends on the live `aim-read` page and its real article API responses
- local video and subtitle processing remain browser-local
- public release contents intentionally exclude local smoke scripts, auth state, test media, proxy-only traces, and private workspace artifacts
